#include <iostream>
#include <map>
#include <vector>
#define ll long long int
#include "l1_cache.cpp"


struct data_bus{
    ll data;
    ll address;
    int core_id; // core id
    int type; // type of the transaction (read/write)
}
class mesi
{
private:
    /* data */
public:
    mesi(/* args */);
    ~mesi();
};




